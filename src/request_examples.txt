user_id указывать в пределах от 200 до 168552, при этом около 5000 id не занято, 
при вводе несуществующего польлзователя получаем ошибку (так сделано осознанно т.к. по условию задания 
у нас нет новых пользователей, база неизменна, поэтому выдавать 'среднего' пользователя при отсутствии в базе user_id я не стал)

request_time указывать в пределах от 1.10.2021 до 29.12.2021 - это тоже одно из принятых допущений задания№

Запрос c использованием параметров запроса(query parameters)будет выглядеть так: 
http://localhost:8000/post/recommendations/?id=202&time=2023-01-01T12:00:00&limit=5

где у нас заданы параметры которые можно менять:
?id=202&
time=2023-01-01T12:00:00
limit=5

Запрос с входом в Docker-контейнер будет выглядеть так: 
docker exec -it <CONTAINER ID> bash
- curl "http://localhost:8000/post/recommendations/?id=202&time=2023-01-01T12:00:00&limit=5"
